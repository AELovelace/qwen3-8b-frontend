# title

This action can be used to resume all previously paused sounds that have been paused using [Pause Audio](Pause_Audio.md) or [Pause All Audio](Pause_All_Audio.md). It will have no effect on those sounds that have not been paused
 previously.

 

#### Action Syntax:

#### Example:

The above action block code will check a global variable and if it evaluates to true then all sound is paused, otherwise it is all resumed.
